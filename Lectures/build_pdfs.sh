#! /usr/bin/env bash

# If you have some way to build PDFs (or copy them into place) then this is the script to manipulate them
# It should still mkdir even if nothing else.

mkdir -p static_slides/PDFs
