# MkSlides config — the reveal-md successor for EMSC-3002 slide decks.
# Mirrors the old reveal-md flags from build_slides.sh so existing *.reveal.md render.
slides:
  theme: css/anu.css        # local ANU theme (was fetched from the live gh-pages URL)
  highlight_theme: github
  separator: "<--o-->"       # was reveal-md --separator
  separator_vertical: "<--v-->"   # was reveal-md --vertical-separator
  separator_notes: "^Note:"  # a "Note:" line starts reveal speaker notes

revealjs:
  width: 1200
  height: 800
  margin: 0.07
  transition: fade
  slideNumber: true
  hash: true          # honour #/h/v fragments so the glossary can deep-link to slides
  # Render maths from the vendored KaTeX in slideshows/katex rather than the
  # plugin's default CDN, so decks work with no network in the lecture theatre.
  # The plugin appends /dist/katex.min.{js,css} to this path. Staged by build.sh.
  katex:
    local: katex
